language: python

python :
  -"nightly"

install:
  - pip install mkdocs-material
script :
  - mkdocs build
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $dep
  keep-history: true
  local-dir: site
  target-branch: page
  on:
    branch: main
